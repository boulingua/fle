# MIGRATION_NOTES.md

Verification + integration pass following the Quarto → Hugo migration and the
Materials Discovery Network rollout.

**Repo:** `boulingua/fle` · **Site type:** FLE BW (French, Gesamtschule BW, Klassen 6–13)
**Author of all content:** S. Le Boulanger
**Branch:** `main` · **Lang:** `fr`
**Author identity for all commits:** `s-leboulanger <277736839+s-leboulanger@users.noreply.github.com>`
**Date opened:** 2026-05-06

## Adapted prompt — phase mappings

| Generic | This repo |
|---|---|
| qmd → Hugo conversion | done; see `MIGRATION_PLAN.md` |
| Network viz at `/network/` | Materials Discovery Network at `/materiel/`; see `MATERIALS_NETWORK_PLAN.md` |
| Lesson front matter | Unit pages under `content/track_*/units/*.md` carry `title`, `subtitle`, `niveau`, `klassenstufe`, `track`, `unit_nr`, `slug`, `bildungsplan`, `skills_focus`, `presentation`, `worksheet`, `vgwort_pixel`, `tags`, `aliases` |
| `data/vgwort.yaml` | Per-page `vgwort_pixel:` frontmatter + repo-root `vgwort-manifest.csv` (193 entries, 1:1 verifier in CI) |
| Ressourcen-Hub gates (commercial / license) | **N/A** — this repo is not a Ressourcen-Hub site |
| CEFR-level enforcement (DaF Goethe) | **N/A** — this repo uses BW Niveau (E / M / G), not CEFR levels |
| Bildungsplan BW live-fetch | **In scope** — this is FLE BW |

---

## Phase 0 — Detection pass (CI gates inventory)

### Pre-migration (Quarto era) workflow `publish.yml`

Reconstructed from `git show 49ce54c:.github/workflows/publish.yml`:

| Step | Purpose | Survived migration? |
|---|---|---|
| Quarto setup + tinytex | Build engine | replaced by Hugo build (PDF subset retained) |
| **Impressum / Datenschutz TODO gate** | `grep -E "<TODO" impressum.qmd datenschutz.qmd` — fail on unfilled placeholders | **NO — lost.** Phase 6 must restore. |
| `quarto render` | Render HTML + Reveal.js decks + exam PDFs | replaced by Hugo (HTML) + slim Quarto-PDF (exams) |
| Organise exam PDFs into canonical paths | `_scripts/organise_downloads.sh` | retained (still in `_scripts/`) |
| Generate placeholder worksheet PDFs | `_scripts/make_placeholder_worksheets.py` | retained (still wired in CI, `continue-on-error`) |
| Verify 156 exam + 156 worksheet PDFs | Count check | partial — 156 worksheet PDFs verified to exist; exam PDFs are non-blocking |
| **Verify every PDF attributes S. Le Boulanger** | `pypdf` author-string check | **partial** — only inside the non-blocking exam-render step. New worksheet PDFs (generated by `make_materials.py`) set `author="S. Le Boulanger"` on creation but no CI gate audits them post-build. |
| Lua filter `_scripts/vgwort.lua` | VG Wort Zählpixel emission | replaced by `layouts/_partials/vgwort.html` driven by frontmatter; `vgwort-manifest.csv` + `scripts/verify-vgwort.sh` enforces 1:1 mapping (193/193 in CI) |

### Current (Hugo era) workflow `hugo.yml`

| Step | Purpose | Status |
|---|---|---|
| Plausible-in-source gate | grep partial | green |
| Plausible-in-rendered-home gate | grep `public/index.html` | green |
| VG Wort 1:1 verifier | `scripts/verify-vgwort.sh vgwort-manifest.csv public` | 193/193 |
| `python _scripts/build_graph.py` | Materials Network graph + 4 CI gates (zero-tag, unknown-topic, zero-edge, orphan-topic, unknown-node-topic) | green |
| Pagefind index | `npx pagefind --site public`; fail if `public/pagefind/pagefind.js` missing | green |
| Bundle size budget | sum gzipped local JS for `/materiel/`; ≤ 280 KB | 6.5 KB used |
| Lighthouse a11y ≥ 95 | blocking | exactly 95 |
| Lychee broken-link audit | advisory (`continue-on-error`, excludes `/fle/`) | passes advisory |
| Exam PDF render (Quarto + TinyTeX) | non-blocking; PDF author audit only inside this step | flaky externally |

### Gates that need re-implementation in Phase 6

1. **Impressum / Datenschutz placeholder gate** — lost. The qmd version greps for `<TODO`. Hugo equivalent must scan the rendered `public/impressum/index.html`, `public/datenschutz/index.html`, `public/haftungsausschluss/index.html` for forbidden placeholder strings (`[NAME]`, `[ADDRESS]`, `TODO`, `XXX`, `Lorem ipsum`, `[STUB]`, `<TODO`). Hard-fail.
2. **Bildungsplan BW live-fetch** — never existed; new gate per the prompt. Every unit's `bildungsplan:` entries (curriculum-anchor strings like `"3.1.3.3 Expression orale — interaction"`) must resolve against `bildungsplaene-bw.de`. Hard-fail on any 404 / fetch failure. Cache the live response, never substitute.
3. **HTML author attribution gate** — never existed. Every rendered article page must contain `S. Le Boulanger` in `<meta name="author">` and visibly on the page (footer attribution + frontmatter `author:`). The Coder theme + footer partial handle this site-wide via `Site.Params.author`; need a gate that scans rendered HTML and fails if any article page is missing the name.
4. **PDF author attribution audit** — exists only inside the non-blocking exam-render step. Should run unconditionally over `static/downloads/` + `static/materials/worksheets/` and hard-fail if any PDF lacks `/Author=Le Boulanger` (case-insensitive substring match). Decoupled from exam-render flakiness.
5. **Front-matter `author:` field** — currently the canonical attribution comes from `Site.Params.author = "S. Le Boulanger"`, with no per-page `author:` frontmatter. Per the prompt's "no page may be missing the author" rule, either keep the site-wide default + a build-time check, or backfill `author:` per file. Recommendation: site-wide default + a frontmatter-presence-NOT-required gate (every rendered HTML must contain the author string, which is guaranteed by the Coder theme via head meta + footer).

## Phase 1 — Post-conversion content verification

### 1.1 Front-matter audit (`scripts/audit_content.py`)

200 `content/**/*.md` files audited (156 unit pages + 13 track index + 13 uebersicht + 10 top-level + 5 annexes + 3 materiel hub pages).

**Hard failures: 0.** All unit pages carry the required fields (`title`, `niveau`, `klassenstufe`, `track`, `unit_nr`, `bildungsplan`, `skills_focus`, `presentation.file`, `worksheet.file`, `vgwort_pixel`).

**Soft warnings (47):**
- 44 non-unit pages have no `tags:` — by design. Tags exist for the Materials Network's facet rail; non-unit pages aren't part of the network.
- 3 pages have ≥1800 chars without `vgwort_pixel`: `content/_index.md` (homepage), `datenschutz.md`, `haftungsausschluss.md`. **Intentionally unregistered** — these are hub/legal pages, not editorial content. The Phase 5 (Materials Network) prompt also explicitly excludes hub/navigation pages from VG Wort coverage.

CI: wired into `hugo.yml` as the **Audit content** step, immediately after `build_graph.py`. Hard-fails on any missing required frontmatter, leftover Quarto fenced div, leftover Pandoc cross-ref, leftover Quarto execution YAML, or unresolved image asset.

### 1.2 Hugo build warnings

`hugo --gc --minify --printPathWarnings --printUnusedTemplates` produces 13 WARNs, all of the form `Template /_partials/taxonomy/<x>.html is unused` from the upstream Coder theme. These are theme-internal taxonomy templates we don't use (`tags`/`categories`/`authors`); our overrides cover everything needed. Untouchable, expected, no action.

No PathWarnings (broken intra-Hugo asset references).

### 1.3 Content parity

| Source | Pre-migration | Hugo today | Delta |
|---|---|---|---|
| Migrated `.qmd` → `.md` | 197 | 197 | 0 |
| `_exam.qmd` (PDF-only, retained) | 156 | 156 | 0 |
| Materials Network hub pages (new) | 0 | 3 | +3 (materiel/_index, materiel/fiches/_index, materiel/presentations/_index) |
| `materiel/preview/` | 1 | 0 | -1 (promoted to `/materiel/`) |

Total `content/**/*.md` = 200 (197 migrated + 3 hub). Matches expectation.

### 1.4 Code execution carryover

Phase 0 inventory of the Quarto source already established: **0** executable code blocks (`{r}`, `{python}`), **0** includes, **0** listing pages, **0** tabsets in any `.qmd`. The `_freeze/` and `.quarto/` execution caches were never tracked. Nothing to regenerate.

### 1.5 Asset paths

The audit script's image-resolution check passes for all 200 files (no `unresolved image path` failures). Static assets under `static/materials/` (156 × 4 = 624 placeholder files) verified by the `make_materials.py` round-trip in Phase 3 of the network rollout.

---

### Gates explicitly out of scope (per repo type FLE BW)

- CEFR-level enforcement (`A1..C2`) — this repo uses BW Niveau (`E`/`M`/`G`), already gated by `_scripts/build_graph.py` via `skills_focus`.
- Commercial source exclusion / license taxonomy — not a Ressourcen-Hub site.
- `data/source_allowlist.yaml` / `data/commercial_denylist.yaml` — not applicable.
