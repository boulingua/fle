# Migration Plan — Quarto → Hugo (Coder)

**Repo:** `boulingua/fle` · **Default branch:** `main` · **Lang:** `fr` · **Site title:** `FLE — S. Le Boulanger`
**Reference site:** https://github.com/boulingua/website (Hugo + `luizdepra/hugo-coder` via Hugo Modules)
**Date opened:** 2026-05-06 · Author identity for all commits: `s-leboulanger <277736839+s-leboulanger@users.noreply.github.com>`

---

## 1. Inventory summary

### 1.1 Content totals

| Bucket | Count |
|---|---|
| Total `.qmd` files | **353** |
| Unit pages (HTML articles) | **156** |
| Exam pages (PDF-only outputs) | **156** |
| Track landing pages (`index.qmd`) | 13 |
| Track weekly plans (`uebersicht.qmd`) | 13 |
| Top-level pages | 10 (`index`, `a_propos`, `bildungsplan`, `programme`, `references`, `bibliographie`, `remerciements`, `impressum`, `datenschutz`, `haftungsausschluss`) |
| Annexes | 5 (`competences`, `erreurs_typiques`, `glossaire`, `grille_evaluation`, `strategies`) |

### 1.2 Tracks

- **Filière G+M (Hauptschulabschluss / Mittlere Reife):** `track_gm_kl06` … `track_gm_kl10` — 5 tracks × 12 units = **60 units**.
- **Filière E (Abitur):** `track_e_kl06` … `track_e_kl13` — 8 tracks × 12 units = **96 units**.
- Each unit has both a `unitNN_<slug>.qmd` (HTML article) and a `unitNN_<slug>_exam.qmd` (PDF-only).

### 1.3 Static assets / supporting files

| Path | Contents |
|---|---|
| `assets/` | `_shared.scss`, `light.scss`, `dark.scss`, `slides.scss` |
| `custom.scss`, `styles.css` | top-level theme overrides |
| `_includes/` | `_exam.tex` (LaTeX header for exam PDFs) |
| `_resources/` | `bildungsplan_bw_franzoesisch.yml`, `curriculum_outline.yml`, `generation_log.yml` |
| `_scripts/` | `vgwort.lua` (filter), Python build helpers (`make_placeholder_worksheets.py`, `pdf_attribution.py`, `organise_downloads.sh`, etc.) |
| `docs/` | rendered Quarto output (gitignored-style; will be deleted in Phase 4) |
| `scripts/` | (small dir, contents unverified — list in Phase 1) |

### 1.4 Quarto features in use

| Feature | Count / status | Notes |
|---|---|---|
| `::: {.callout-*}` blocks | **177 files** | Direct Hugo equivalent needed (custom shortcode). |
| `::: {.panel-tabset}` | **0** | None — no tabset migration needed. |
| Executable code blocks (`{r}`, `{python}`) | **0** | None. |
| `{{< include … >}}` | **0** | None. |
| Listing pages (`listing:` frontmatter) | **0** | None. |
| Custom div classes | many (`.hero-kicker`, `.lead`, `.card-grid`, `.card`, `.cefr-badge`, `.vgwort-pixel`) | Each maps to a CSS class in the new theme. |
| Cross-format output | **156 exam pages → PDF only** | Frontmatter `format: pdf` only. These are not HTML pages — they are PDF artifacts dropped into `docs/downloads/`. |
| Reveal.js slides | implied by README (`/programme.qmd` mentions decks); no `format: revealjs` blocks found in frontmatter at top of sampled units → likely generated by another mechanism (worker script). **Open question — verify in Phase 1.** |
| Pandoc Lua filter | `_scripts/vgwort.lua` (currently inactive — see §3) | Filter wired in `_quarto.yml`, but body uses raw-HTML `::: {.vgwort-pixel}` blocks instead of the metadata key the filter expects. The filter is effectively a no-op today. |

### 1.5 Tracking inventory (CRITICAL)

**Plausible Analytics — exact snippet from `_quarto.yml` lines 102–104:**

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Source+Sans+3:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<script defer data-domain="boulingua.github.io/fle" src="https://analytics.hellebo.de/js/script.file-downloads.outbound-links.js"></script>
<script>window.plausible = window.plausible || function() { (window.plausible.q = window.plausible.q || []).push(arguments) }</script>
```

The reference site uses the **identical** pattern with a different `data-domain` (`boulingua.github.io/website`).

**VG Wort pixels — 349 unique pixels found across 349 `.qmd` files.**

Embedding pattern (verbatim, repeated per file with a different 32-hex token):

```markdown
::: {.vgwort-pixel}
```{=html}
<!-- VG Wort Zählmarke (slb) — public ID: <32-hex-token> -->
<img src="https://vg09.met.vgwort.de/na/<32-hex-token>"
     width="1" height="1" alt=""
     loading="eager"
     fetchpriority="high"
     decoding="async"
     style="position:absolute;visibility:hidden;" />
```
:::
```

**Files without a VG Wort pixel (4):** must be confirmed — likely legal pages (`impressum`, `datenschutz`, `haftungsausschluss`) and/or `index.qmd`. To be enumerated in the manifest at the top of Phase 2.

**The lua filter `_scripts/vgwort.lua` is currently a no-op** — it expects `vgwort_pixel:` in YAML frontmatter, but the actual pixels live in body div blocks. Migration must preserve the **body-embedded pixels**, not the filter machinery.

---

## 2. Mapping table — Quarto → Hugo

| Quarto construct | Hugo replacement | Notes |
|---|---|---|
| `_quarto.yml` site config | `hugo.toml` (TOML) | Match reference repo structure. |
| `format.html.theme: [flatly, …]` | Coder theme via Hugo Modules + `assets/css/custom.css` | Port SCSS overrides into Coder's customisable variables; preserve light/dark. |
| Navbar `website.navbar.left/right` | `[[menu.main]]` / `[[menu.footer]]` blocks | Preserve full structure incl. submenus. |
| Footer `website.page-footer` | `layouts/_partials/footer.html` (override Coder's) | Verbatim text + links. |
| `.qmd` body markdown | `.md` body (Goldmark, `unsafe = true`) | Verbatim copy. |
| `::: {.callout-note}` etc. | Custom shortcode `{{< callout type="note" >}}…{{< /callout >}}` (`layouts/shortcodes/callout.html`) | Render to same Bootstrap-callout HTML; preserve types `note`, `tip`, `warning`, `important`, `caution`. |
| `::: {.hero-kicker}`, `.lead`, `.card-grid`, `.card`, `.cefr-badge` | Plain `<div class="…">` via raw HTML (Goldmark `unsafe`) **or** thin shortcodes | Preserve class names; port matching CSS. |
| `[Text]{.cefr-badge.a1}` (Pandoc bracketed span) | Raw HTML `<span class="cefr-badge a1">Text</span>` | Goldmark does not parse Pandoc bracketed spans; rewrite at migration time. |
| Cross-refs `[Text](page.qmd)` | `[Text]({{< ref "page.md" >}})` or relative `[Text](../page/)` | Use `ref` shortcode for resolved-at-build links. |
| Images `![](path)` | Same; assets moved to `assets/` (processed) or `static/` (raw). | |
| `format: pdf` exam files | Out of Hugo's scope. **Keep building them via the existing Quarto-PDF pipeline** as a separate CI step that emits PDFs into `static/downloads/<track>/<unit>_exam.pdf`. Hugo then serves them as static assets. | This is the cleanest split: Hugo for HTML, Quarto-LaTeX for the 156 exam PDFs. Confirm this approach before Phase 2. |
| `_scripts/vgwort.lua` filter | **Discarded** (it is a no-op today). | |
| Plausible script tag | `layouts/_partials/head/plausible.html` (mirrors reference repo's `extensions.html`) | Verbatim copy of the snippet, only `data-domain` stays as-is. |
| VG Wort `::: {.vgwort-pixel}` body block | `layouts/_partials/vgwort.html` partial reading `Params.vgwort_pixel` (token) and emitting the same `<img>`; injected at end of `single.html`. | Migrator extracts the 32-hex token from each `.qmd` and writes it as `vgwort_pixel: <token>` in the new `.md` frontmatter. The body block is deleted. CI verifier confirms 1:1 token mapping. |

---

## 3. Navigation plan

**Top-level (`[[menu.main]]`):**

1. Accueil (`/`)
2. À propos (`/a-propos/`)
3. Référentiel (`/bildungsplan/`)
4. Programme (`/programme/`)
5. Références (`/references/`)
6. **Matériel** (new — top-level dropdown)
   - Présentations (`/materiel/presentations/`)
   - Fiches d'exercices (`/materiel/fiches/`)
7. Filière G+M (cl. 6–10) — submenu of 5 entries
8. Filière E (cl. 6–13) — submenu of 8 entries

**Right-side menu:**

- Mentions légales: Impressum / Protection des données / Avertissement
- GitHub icon → `https://github.com/boulingua/fle`

**Footer:** verbatim from `_quarto.yml` lines 65–84, only converting `.qmd` paths to clean Hugo URLs.

**Localisation of "Materials" per the prompt's per-site rule:** for `fle` the label is **`Matériel`**.

---

## 4. Risk list / open questions

1. **Reveal.js decks.** The README and `index.qmd` advertise Reveal.js slides per unit, but no `format: revealjs` was found in unit frontmatter. The slide generation must live somewhere I have not located yet — possibly an unrun phase. **Decision needed:** is slide rendering currently active? If yes, where? If no, is the new Hugo site expected to produce slides at all?
2. **Exam PDFs.** 156 `_exam.qmd` files render to PDF via LaTeX (`_includes/_exam.tex`). Hugo cannot do this. **Recommended split:** keep a slimmed-down Quarto-PDF step in CI that produces only `*_exam.pdf` into `static/downloads/`, and let Hugo serve them as static files. Confirm before Phase 2.
3. **Phase 3 placeholder materials clash with existing real worksheet placeholders.** `_scripts/make_placeholder_worksheets.py` already produces worksheet PDFs in CI. The migration prompt instructs me to create dummy `.pptx` + `.pdf` per article in `static/materials/`. **Decision:** do new placeholders supersede the existing worksheet pipeline, or do we wire the existing worksheet outputs into the Materials hub and only generate `.pptx` placeholders fresh? Strong recommendation: reuse existing worksheets, generate `.pptx` placeholders fresh.
4. **VG Wort pixels on listing pages.** Confirmed in prompt (no pixels on Materials hub). Will not pixel: `/`, `/materiel/*`, track index/uebersicht pages? Track index/uebersicht **already have pixels today** (e.g. `track_e_kl06/uebersicht.qmd` has `1be38254…`). They are revenue-bearing. **They keep their pixels.** Only the *new* hub pages (`/materiel/presentations/`, `/materiel/fiches/`) go un-pixeled.
5. **`docs/` directory is the current Quarto output dir.** It is also where the reference Hugo site would write to. Must verify `docs/` is not gitignored (it appears tracked) before deletion in Phase 4. GitHub Pages may currently serve from `docs/` on `main` rather than from a `gh-pages` branch — must check repo Pages settings or workflow. The current `publish.yml` actually uses `actions/upload-pages-artifact` against `docs/` then `deploy-pages`, so it is **artifact-based deployment**, not branch-based. The new Hugo workflow should match (artifact-based, building to `public/` by default).
6. **Content not pixeled today (4 files of 353).** Need explicit list confirmed in Phase 2 manifest before assuming legal pages are intentionally excluded.
7. **Lua filter on `_quarto.yml` line 86–87 is a no-op.** I will not port it, but want to flag in case the user expected it to be doing something.
8. **Pandoc bracketed spans `[X]{.cefr-badge.a1}`** appear in track index pages. Goldmark will not parse these — they must be rewritten to raw HTML at migration time. There may be other Pandoc-only syntax (e.g. `{#sec-…}` heading IDs) that needs a sweep.

---

## 5. File-by-file migration order (Phase 2)

To allow incremental validation:

1. **Top-level** (10 files): `index`, `a_propos`, `bildungsplan`, `programme`, `references`, `bibliographie`, `remerciements`, `impressum`, `datenschutz`, `haftungsausschluss`.
2. **Annexes** (5 files): `competences`, `erreurs_typiques`, `glossaire`, `grille_evaluation`, `strategies`.
3. **Track landings × 13** (`track_*/index.qmd`).
4. **Track weekly plans × 13** (`track_*/uebersicht.qmd`).
5. **Units, batched per track**, in this order: `track_gm_kl06` → `…kl10`, then `track_e_kl06` → `…kl13`. 12 units per track. **Exam `.qmd` files are NOT migrated to Hugo** — they remain Quarto-PDF inputs (per §4 risk 2, pending confirmation).
6. After each batch: word-count diff vs source, log mismatches in this file under "Manual review needed".

---

## 6. Phase log

### Phase 0 — 2026-05-06

- Inventory complete (this document).
- VG Wort embedding pattern characterised: 349 in-body pixels, no use of the lua filter's metadata interface.
- Reference repo cloned to `/tmp/boulingua-reference`; theme = `luizdepra/hugo-coder` via Hugo Modules; Plausible snippet location = `layouts/_partials/head/extensions.html`.
- **Pending decisions before Phase 2 begins:**
  - §4-1 Reveal.js slides: keep, drop, or defer?
  - §4-2 Exam PDFs: confirm hybrid Quarto-PDF + Hugo-HTML approach?
  - §4-3 Materials hub: reuse existing worksheet pipeline?
- **No commits made.** No files outside this `MIGRATION_PLAN.md` were modified.
